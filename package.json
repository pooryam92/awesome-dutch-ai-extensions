{
  "name": "awesome-dutch-ai-extensions",
  "version": "0.1.0",
  "private": true,
  "description": "Curated index of Dutch AI extensions (MCP, skills, plugins).",
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "generate": "node scripts/generate.mjs",
    "build": "node scripts/validate.mjs && node scripts/generate.mjs",
    "check": "node scripts/validate.mjs && node scripts/generate.mjs --check"
  },
  "license": "MIT",
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
